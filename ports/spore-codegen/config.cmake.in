get_filename_component(_IMPORT_PREFIX "${CMAKE_CURRENT_LIST_FILE}" PATH)
get_filename_component(_IMPORT_PREFIX "${_IMPORT_PREFIX}" PATH)
get_filename_component(_IMPORT_PREFIX "${_IMPORT_PREFIX}" PATH)

set(SPORE_CODEGEN_FOUND TRUE)
set(SPORE_CODEGEN_SCRIPTS "${_IMPORT_PREFIX}/share/@PORT@")
set(SPORE_CODEGEN_EXECUTABLE "${_IMPORT_PREFIX}/tools/@PORT@/@PORT@@VCPKG_TARGET_EXECUTABLE_SUFFIX@")

add_executable(spore::@PORT@ IMPORTED)

set_target_properties(
  spore::@PORT@
  PROPERTIES
    IMPORTED_LOCATION ${SPORE_CODEGEN_EXECUTABLE}
    IMPORTED_CONFIGURATIONS Release
    INTERFACE_COMPILE_DEFINITIONS
        SPORE_WITH_CPP=${SPORE_WITH_CPP}
        SPORE_WITH_SPIRV=${SPORE_WITH_SPIRV}
)

unset(_IMPORT_PREFIX)