get_filename_component(_IMPORT_PREFIX "${CMAKE_CURRENT_LIST_FILE}" PATH)
get_filename_component(_IMPORT_PREFIX "${_IMPORT_PREFIX}" PATH)
get_filename_component(_IMPORT_PREFIX "${_IMPORT_PREFIX}" PATH)

set(SPORE_EMBED_FOUND TRUE)
set(SPORE_EMBED_SCRIPTS "${_IMPORT_PREFIX}/share/@PORT@")
set(SPORE_EMBED_EXECUTABLE "${_IMPORT_PREFIX}/tools/@PORT@/@PORT@@VCPKG_TARGET_EXECUTABLE_SUFFIX@")

add_executable(spore::@PORT@ IMPORTED)

set_target_properties(
  spore::@PORT@
  PROPERTIES
    IMPORTED_LOCATION ${SPORE_EMBED_EXECUTABLE}
    IMPORTED_CONFIGURATIONS Release
)

unset(_IMPORT_PREFIX)