The package spore-meta provides CMake scripts and an interface CMake target:

    find_package(spore-meta CONFIG REQUIRED)

    list(APPEND CMAKE_MODULE_PATH ${SPORE_META_SCRIPTS})

    include(SporeMeta)

    spore_add_meta(target)
